spring:
  data:
    mongodb:
      uri: ${AZURE_COSMOS_MONGO_CONNECTION_STRING}
      database: ${AZURE_COSMOS_MONGO_DATABASE}
  cloud:
    config:
      enabled: false
#server:
#  port: 8081
app:
  client:
    mjml:
      url: "https://dev-kinect-apim-service.azure-api.net/mjml/v1/mjml2html"
      api-key: ${AZURE_APIM_KEY}
